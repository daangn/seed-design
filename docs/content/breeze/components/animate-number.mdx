---
title: Animate Number
description: 숫자를 부드럽게 애니메이션하는 유틸리티 컴포넌트
whenToRead: 카운터처럼 바뀌는 숫자에 애니메이션을 넣으려고 motion 의존성과 CLI 스니펫(breeze:animate-number)을 설치하고 LazyMotion 기반 MotionProvider를 설정하거나, 쉼표·그라디언트 마스크 props를 조정할 때 읽는다. SEED 정식 컴포넌트가 아닌 Breeze 유틸리티다.
---

<ComponentExample name="breeze/animate-number/preview">
  ```json doc-gen:file
  {
    "file": "examples/breeze/animate-number/preview.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

## Installation

Dependency:

```package-install
npm install motion
```

Snippet:

```package-install
npx @seed-design/cli@latest add breeze:animate-number
```

<BreezeManualInstallation name="animate-number" />

## Configuration

animate-number 컴포넌트는 motion의 `m.` 컴포넌트를 사용합니다. lazy loading을 위해 프로젝트에 MotionProvider를 설정해야 합니다.

자세한 내용은 [Motion Lazy Motion 문서](https://motion.dev/docs/react-lazy-motion)를 참고하세요.

```tsx title="MotionProvider.tsx"
"use client";

import { LazyMotion } from "motion/react";
import type { ReactNode } from "react";

const loadFeatures = () => import("motion/react").then((res) => res.domAnimation);

export function MotionProvider({ children }: { children: ReactNode }) {
  return <LazyMotion features={loadFeatures}>{children}</LazyMotion>;
}
```

```tsx title="index.tsx"
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import { MotionProvider } from './MotionProvider.tsx'

createRoot(document.getElementById('root')!).render(
  <MotionProvider>
    <App />
  </MotionProvider>
)
```

## Props

<react-type-table path="./registry/react/breeze/animate-number/animate-number.tsx" name="AnimateNumberProps" />

## Examples

### Comma

<ComponentExample name="breeze/animate-number/comma">
  ```json doc-gen:file
  {
    "file": "examples/breeze/animate-number/comma.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Gradient

<ComponentExample name="breeze/animate-number/gradient">
  ```json doc-gen:file
  {
    "file": "examples/breeze/animate-number/gradient.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Custom Style

<ComponentExample name="breeze/animate-number/custom-style">
  ```json doc-gen:file
  {
    "file": "examples/breeze/animate-number/custom-style.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>
