---
title: Screen Edges
description: 화면 가장자리는 시스템 UI나 기기 형태에 가려질 수 있습니다. 요소를 가려지지 않는 안전 영역(Safe Area) 안에 둘지, 화면 끝까지 채울지 정하는 기준을 설명합니다.
---

## 안전 영역

안전 영역(Safe Area)은 화면에서 시스템 UI나 기기의 형태에 가려지거나 잘리지 않는 영역입니다. 안전 영역 밖은 다음 요소에 의해 가려지거나 잘릴 수 있습니다.

- 상태 표시줄, 홈 인디케이터처럼 화면 위에 겹쳐 표시되는 시스템 UI
- 노치, Dynamic Island, 카메라 구멍처럼 카메라와 센서가 차지한 부분
- 화면의 둥근 모서리

<FigmaImage
  id="4293:1743"
  alt="같은 기기의 세로 화면과 가로 화면에서 안전 영역 밖을 반투명 면으로 표시한 그림"
/>

### 네 변의 크기는 서로 다를 수 있습니다

안전 영역의 inset은 위아래에만 있지 않습니다. 기기 형태에 따라 세로 화면에서도 왼쪽이나 오른쪽에 inset이 생길 수 있습니다. 화면이 가로 방향으로 회전하면 위쪽에 있던 노치나 카메라가 왼쪽이나 오른쪽에 놓일 수 있습니다. 홈 인디케이터가 가로 화면의 아래쪽에 표시되는 기기에서는 하단 inset도 남을 수 있습니다. 실제 배치는 기기와 OS, 화면 회전 지원 여부에 따라 달라집니다.

네 변의 inset은 서로 독립적입니다. 위와 아래, 왼쪽과 오른쪽의 값이 같다고 가정하지 않습니다.

### 크기는 상황마다 달라집니다

안전 영역의 크기는 기기, OS, 화면 방향, 시스템 UI의 표시 상태에 따라 달라질 수 있습니다. 네 방향의 inset이 모두 0일 수도, 일부만 0이 아닐 수도, 모두 0이 아닐 수도 있습니다.

따라서 모든 화면에서 위·아래·왼쪽·오른쪽 inset을 각각 확인해 적용합니다. 특정 방향이 항상 0이라고 가정하거나, 특정 기기에서 확인한 값을 모든 화면에 고정값으로 적용하지 않습니다.

### 화면을 띄운 뒤에도 달라질 수 있습니다

화면이 이미 띄워진 상태에서도 안전 영역의 크기나 위치가 달라질 수 있습니다.

- 크기가 달라지는 경우: 화면 분할을 지원하는 환경에서는 앱이 차지하는 영역이 달라지면서 각 변의 inset이 줄어들거나 없어질 수 있습니다.
- 위치가 달라지는 경우: 양쪽 가로 방향을 지원하는 환경에서는 기기를 180도 돌렸을 때 카메라를 피하기 위한 inset이 반대쪽으로 옮겨 갈 수 있습니다.

따라서 화면을 띄울 때 잰 안전 영역을 계속 쓰지 않고, 안전 영역이 바뀌면 배치도 다시 맞춥니다.

## 콘텐츠는 안전 영역 안에

누를 수 있는 요소 및 가려지거나 잘렸을 때 정보나 기능을 조금이라도 잃을 수 있는 콘텐츠는 안전 영역 안에 두어야 합니다.

- 누를 수 있는 요소: 버튼, 링크, 입력 필드, 탭
- 읽어야 하는 정보: 텍스트, 아이콘, 가격, 배지, 의미를 전달하는 이미지

<Grid>
  <DoImage
    figmaId="4303:363"
    body="텍스트와 버튼은 안전 영역 안에 배치합니다."
    alt="가로 화면의 목록과 하단 버튼이 카메라와 화면 가장자리를 피해 안전 영역 안에 배치된 모습"
  />
  <DontImage
    figmaId="4303:410"
    body="콘텐츠를 화면 끝부터 배치하면 카메라나 홈 인디케이터와 겹칠 수 있습니다."
    alt="가로 화면의 목록이 카메라와 겹치고 하단 버튼이 안전 영역 밖까지 내려간 모습"
  />
</Grid>

### 여백은 안전 영역 경계부터 잽니다

요소가 원래 갖는 여백은 화면 끝이 아니라 안전 영역 경계부터 잽니다.

- 좌우: 콘텐츠를 안전 영역 경계에서 [`$dimension.spacing-x.global-gutter`](/foundations/spacing#global-gutter)만큼 떨어뜨립니다.
- 아래: 하단 고정 바의 버튼을 하단 안전 영역 경계에서 바의 안쪽 여백만큼 떨어뜨립니다.

여백을 화면 끝부터 재면, 안전 영역이 여백보다 넓은 곳에서 콘텐츠가 가려집니다. 반대로 안전 영역이 여백을 대신하게 하면 글자가 노치나 카메라 바로 옆에 붙습니다.

<FigmaImage
  id="4343:648"
  alt="가로 화면의 왼쪽 가장자리를 확대해 안전 영역 경계와 콘텐츠 사이의 $dimension.spacing-x.global-gutter를 표시한 그림. 배너 배경은 화면 끝까지 이어집니다."
/>

### 가운데 정렬은 안전 영역을 기준으로 합니다

네 변의 크기가 서로 다를 때, 가운데 정렬하는 요소는 화면이 아니라 안전 영역의 가운데에 둡니다. 따라서 안전 영역이 한쪽에만 있으면, 요소는 화면 중심에서 안전 영역이 없는 쪽으로 조금 옮겨 갑니다.

## 배경은 화면 끝까지

가려지거나 잘려도 잃는 것이 없는 요소는 안전 영역 밖까지, 화면 끝까지 채웁니다. 배경이 안전 영역에서 멈추면 안전 영역 밖이 빈 띠로 남아 화면이 좁아 보이고, 화면을 가로지르는 요소가 중간에 끊겨 보입니다.

- 배경색이 있는 섹션과 배너: 좌우를 화면 끝까지 채웁니다.
- [Full-width Divider](/components/divider): 좌우를 화면 끝까지 채웁니다.
- 히어로 이미지처럼 장식으로 쓰는 이미지: 좌우를 화면 끝까지 채웁니다.
- 지도, 동영상, 전체 화면 이미지 뷰어: 표시 영역의 배경을 네 변 모두 화면 끝까지 채웁니다. 이미지나 동영상 자체의 비율과 잘림 여부는 콘텐츠의 목적에 맞게 정합니다.

<Grid>
  <DoImage
    figmaId="4343:875"
    body="배너 배경과 탭의 스크롤 영역은 화면 끝까지 이어지게 합니다."
    alt="가로 화면에서 Page Banner의 배경과 탭 목록이 좌우 화면 끝까지 이어지고 배너 글자는 안전 영역 안에 있는 모습"
  />
  <DontImage
    figmaId="4343:1093"
    body="배경과 스크롤 영역을 안전 영역 경계에서 끊지 않습니다."
    alt="Page Banner 양옆에 빈 띠가 남고 탭 목록이 오른쪽 안전 영역 경계에서 잘린 모습"
  />
</Grid>

### 배경 위의 콘텐츠

배경과 콘텐츠가 한 요소에 함께 있으면 부분마다 따로 적용합니다. 배경은 화면 끝까지 채우고, 그 위의 콘텐츠는 안전 영역 안에 둡니다.

- 이미지 위에 올린 버튼과 글자: 안전 영역 안에 둡니다.
- 이미지 자체에 들어 있는 글자와 주요 피사체: 안전 영역 안에 오도록 이미지를 구성합니다. 가장자리에는 가려지거나 잘려도 괜찮은 부분을 둡니다.

<Grid>
  <DoImage
    figmaId="4384:150479"
    body="표지 이미지와 본문 배경은 화면 끝까지 채우고, 글은 안전 영역 안에 둡니다."
    alt="아티클 화면에서 계단 표지 사진과 흰색 본문 배경은 오른쪽 끝까지 이어지고, 제목과 본문은 붉게 표시된 오른쪽 inset을 피해 배치된 모습"
  />
  <DontImage
    figmaId="4384:150481"
    body="배경과 함께 글의 영역까지 화면 끝으로 늘리지 않습니다."
    alt="같은 아티클 화면에서 본문을 전체 화면 너비로 배치해 글의 오른쪽 부분이 붉게 표시된 오른쪽 inset과 겹치는 모습"
  />
</Grid>

표지 이미지가 보이지 않을 때까지 스크롤해도 본문은 안전 영역 안에 있어야 합니다.

<Grid>
  <DoImage
    figmaId="4399:126877"
    body="스크롤한 뒤에도 본문이 카메라와 시스템 인디케이터를 피해 읽히도록 합니다."
    alt="표지가 사라질 때까지 스크롤한 아티클의 상단 확대. 본문이 오른쪽 안전 영역을 피해 줄바꿈되어 문장 끝까지 보입니다."
  />
  <DontImage
    figmaId="4399:126879"
    body="본문을 화면 끝까지 펼치면 글자가 카메라와 시스템 인디케이터에 가려집니다."
    alt="같은 본문을 스크롤한 아티클의 상단 확대. 오른쪽으로 이어진 글자가 카메라와 시스템 인디케이터 뒤에 가려집니다."
  />
</Grid>

## 스크롤되는 콘텐츠

스크롤되는 항목은 스크롤하는 동안 안전 영역 밖을 지나갈 수 있습니다. 가려진 항목을 스크롤해서 안전 영역 안으로 다시 가져올 수 있다면 정보나 기능을 잃지 않으므로, 스크롤 영역은 화면 끝까지 넓힙니다. 스크롤 영역을 안전 영역에서 자르면 항목이 화면 안쪽에서 잘려 보입니다.

- 가로 스크롤: [Tabs](/components/tabs), 칩 목록, 캐러셀은 좌우 안전 영역을 지나 화면 밖으로 이어집니다.
- 세로 스크롤: 목록은 화면 아래 끝까지 이어집니다. 하단 inset이 있는 환경에서는 스크롤하는 동안 항목이 안전 영역 밖을 지나갈 수 있습니다.

이때 다음 조건을 지킵니다.

- 스크롤하지 않은 상태에서는 첫 항목을 다른 콘텐츠와 같은 선에 맞춥니다. 가로 스크롤이라면 안전 영역 경계에서 `$dimension.spacing-x.global-gutter`만큼 떨어진 선입니다.
- 화면 끝까지 넓히는 것은 스크롤 영역뿐입니다. 화면에 고정된 버튼과 바는 스크롤해서 가져올 수 없으므로 안전 영역 안에 둡니다.

<Grid>
  <DoImage
    figmaId="4356:2985"
    body="목록은 하단 안전 영역을 지나 화면 끝까지 이어지게 합니다."
    alt="세로 화면의 하단을 확대한 모습. 점선으로 표시한 안전 영역 경계 아래에도 목록 항목이 이어집니다."
  />
  <DontImage
    figmaId="4356:2986"
    body="스크롤 영역을 하단 안전 영역 경계에서 자르지 않습니다."
    alt="같은 스크롤 위치에서 목록의 글자가 안전 영역 경계의 점선에 걸려 잘리고, 그 아래는 빈 공간으로 남은 모습"
  />
</Grid>

끝까지 스크롤한 상태에서는 마지막 항목 전체가 안전 영역 안에 보여야 합니다. 스크롤 콘텐츠의 끝에 안전 영역 inset과 기본 여백을 함께 확보합니다.

<Grid>
  <DoImage
    figmaId="4362:3784"
    body="끝까지 스크롤하면 마지막 항목과 기본 여백이 안전 영역 안에 남도록 합니다."
    alt="끝까지 스크롤한 목록의 하단. 마지막 읽기 목록 행의 제목, 보조 글자, 화살표가 모두 안전 영역 경계 위에 있고 행 아래에도 여백이 남습니다."
  />
  <DontImage
    figmaId="4362:3788"
    body="끝까지 스크롤해도 마지막 항목이 안전 영역 밖에 남지 않도록 합니다."
    alt="하단 여백 없이 끝까지 스크롤한 목록. 마지막 읽기 목록 행의 보조 글자와 화살표가 안전 영역 경계에 걸려 홈 인디케이터 영역에 남습니다."
  />
</Grid>
