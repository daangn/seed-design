---
title: Docs MCP
description: AI Agent가 SEED 문서에 직접 접근할 수 있는 MCP 서버를 제공합니다.
---

## 소개

[`@seed-design/docs-mcp`](https://github.com/daangn/seed-design/tree/dev/packages/docs-mcp)는 SEED 문서를 위한 공식 MCP(Model Context Protocol) 서버입니다.

이 MCP 서버를 사용하면 AI Agent가 다음 정보에 직접 접근할 수 있습니다:

- **컴포넌트**: 해부도, 속성, 사용 가이드라인
- **React · Lynx · Breeze**: 설치, props, 사용 예시
- **Foundations**: 색상, 타이포그래피, 간격 등 디자인 토큰
- **Rootage**: 디자인 토큰과 컴포넌트 스펙 원본 데이터

Figma 연결 없이 독립적으로 실행되며, SEED를 사용하는 프로젝트에서 AI 도구의 문서 이해도를 높일 수 있습니다.

## 설치

<Tabs items={['Claude Code', 'Codex CLI', 'Gemini CLI', 'Cursor', 'Claude Desktop']}>

<Tab value="Claude Code">

```sh
claude mcp add seed-docs -- npx -y @seed-design/docs-mcp
```

</Tab>

<Tab value="Codex CLI">

```sh
codex mcp add seed-docs -- npx -y @seed-design/docs-mcp
```

</Tab>

<Tab value="Gemini CLI">

`~/.gemini/settings.json` 파일에 다음을 추가합니다:

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"]
    }
  }
}
```

</Tab>

<Tab value="Cursor">

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"],
      "type": "stdio"
    }
  }
}
```

[Cursor MCP에 대해 자세히 알아보기](https://docs.cursor.com/context/model-context-protocol)

</Tab>

<Tab value="Claude Desktop">

`~/Library/Application Support/Claude/claude_desktop_config.json` 파일에 다음을 추가합니다:

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"]
    }
  }
}
```

</Tab>

</Tabs>

## 조회할 문서 사이트 바꾸기

기본값은 `https://seed-design.io`입니다. `SEED_DOCS_BASE_URL` 환경 변수로 로컬 문서 서버나 아카이브 사이트를 겨눌 수 있습니다. MCP 클라이언트 설정에서 `env`로 넘깁니다.

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"],
      "env": { "SEED_DOCS_BASE_URL": "http://localhost:3000" }
    }
  }
}
```

## 사용 가능한 도구

서버가 등록하는 도구는 다섯 개입니다.

섹션 목록은 서버 안에 상수로 들어 있지 않고, 문서 사이트가 게시하는 인덱스를 호출 시점에 읽어옵니다. 사이트 구조가 바뀌면 이미 설치된 버전도 그대로 따라가므로, 섹션 이름을 외워서 넘기는 대신 `discover_seed_docs`로 현재 목록을 먼저 확인하세요.

### Discovery

#### `discover_seed_docs`

문서 사이트가 게시하는 섹션 목록을 반환합니다. 섹션마다 id와 이름, 문서 개수가 함께 실립니다.

**파라미터:** 없음

**예시:**
```
discover_seed_docs()
```

**예시 응답 (발췌):**
```json
{
  "totalSections": 10,
  "sections": [
    {
      "id": "components",
      "name": "Components",
      "documentCount": 63
    },
    {
      "id": "react",
      "name": "React",
      "documentCount": 110
    }
  ]
}
```

#### `search_docs`

문서 본문까지 검색해 관련 문서 주소를 관련도 순으로 반환합니다. 어느 섹션에 답이 있는지 모를 때 쓰는 도구이고, 문서 사이트의 검색창과 같은 색인, 같은 순위를 씁니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `query` | string | 찾을 말. 한국어와 영어 모두 동작하며 단어는 공백으로 구분합니다 |

**예시:**
```
search_docs({ query: "액션 버튼" })
```

**예시 응답 (발췌):**
```
# Search: 액션 버튼

Showing the top 20 of 71 matches

- /components/action-button#hierarchy
- /components/floating-action-button
- /react/components/floating-action-button
...
```

주소는 그 문서가 사이트에서 갖는 경로 그대로입니다. 앞의 슬래시를 떼고 그다음 슬래시에서 자르면 앞이 `section`, 뒤가 `get_doc`에 넘길 `path`입니다. `/components/action-button#hierarchy`는 `get_doc({ section: "components", path: "action-button#hierarchy" })`가 됩니다. 조각이 하나뿐인 `/react` 같은 주소는 `section`이 `react`이고 `path`가 빈 문자열입니다.

주소에 붙은 `#앵커`는 어느 제목이 걸렸는지 알려주는 표시일 뿐이고, `get_doc`이 그대로 받아 문서 전체를 반환하므로 떼지 않아도 됩니다.

질의어는 형태소 분석 없이 단어 단위로 맞춰봅니다. 그래서 `액션 버튼`은 찾지만 `액션버튼`은 찾지 못합니다.

---

### 문서

#### `list_docs`

섹션 안의 문서 목록을 반환합니다. 각 항목에 `get_doc`으로 넘길 `path`와 설명, deprecated 표시가 함께 나옵니다. 목록은 `path` 순으로 정렬되므로 같은 경로 아래 있는 문서끼리 모여서 나옵니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `section` | string | 섹션 id. `discover_seed_docs`로 현재 목록을 확인합니다 |

**예시:**
```
list_docs({ section: "react" })
```

**예시 응답:**
```
# React Documentation

Total: 110 documents

- Accordion (path: components/accordion) — 여러 개의 관련된 콘텐츠 섹션을 수직으로 나열하고, 각 섹션을 펼치거나 접어 정보를 탐색할 수 있는 컴포넌트입니다.
- Action Button (path: components/action-button) — 명확한 액션을 쉽게 수행할 수 있도록 돕는 기본 인터랙션 컴포넌트입니다.
...

## Usage

Use get_doc with section="react" and a path above to read a document.
```

없는 섹션을 넘기면 실제 섹션 목록을 담아 거부하므로, 한 번의 재시도로 바로잡을 수 있습니다.

#### `get_doc`

특정 문서의 본문을 반환합니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `section` | string | 섹션 id |
| `path` | string | 섹션 기준 상대 경로 (예: `components/action-button`, `color`). 빈 문자열은 그 섹션 자체의 페이지를 가리키고, `#앵커`가 붙어 있으면 무시합니다 |

**예시:**

컴포넌트의 디자인 스펙:
```
get_doc({ section: "components", path: "action-button" })
```

같은 컴포넌트의 React API:
```
get_doc({ section: "react", path: "components/action-button" })
```

파운데이션 문서:
```
get_doc({ section: "foundations", path: "color" })
```

이름만 넘겼는데 그 이름을 가진 문서가 섹션 안에 둘 이상이면, 앞의 것을 돌려주지 않고 후보 경로를 담아 거부합니다. `react` 섹션의 `alert-dialog`, `bottom-sheet`, `menu-sheet`가 여기에 해당하므로 `components/` 또는 `stackflow/`까지 붙여서 넘겨야 합니다.

`search_docs`가 출력한 주소는 나눈 그대로 넘기면 됩니다. 앵커를 떼거나 섹션 개요 문서의 이름을 따로 찾아볼 필요가 없습니다.

---

### Rootage

SEED의 Rootage 스펙(디자인 토큰 및 컴포넌트 스펙의 원본 JSON 데이터)에 접근합니다.

#### `get_rootage`

Rootage 리소스를 반환합니다. 경로 없이 호출하면 사용 가능한 모든 리소스 목록을 반환합니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `path` | string (선택) | 리소스 경로 (예: `/color.json`, `/components/action-button.json`) |

**예시:**

리소스 목록 조회:
```
get_rootage()
```

특정 리소스 조회:
```
get_rootage({ path: "/components/action-button.json" })
```

경로는 인덱스와 대조한 뒤 가져오므로, `/rootage` 밖을 가리키는 값은 거부됩니다.

---

## 프로그래매틱 사용

`@seed-design/docs-mcp`는 npm 패키지로 설치하여 커스텀 MCP 서버에 통합할 수 있습니다.

### 설치

`server`는 MCP TypeScript SDK v2의 `McpServer` 인스턴스이므로, transport를 직접 연결하려면 `@modelcontextprotocol/server`도 함께 설치합니다.

```bash
npm install @seed-design/docs-mcp @modelcontextprotocol/server
# 또는
bun add @seed-design/docs-mcp @modelcontextprotocol/server
```

### 사용 예시

```javascript
import { StdioServerTransport } from "@modelcontextprotocol/server/stdio";
import { server } from "@seed-design/docs-mcp/server";
import { initializeTools } from "@seed-design/docs-mcp/tools";

// 도구 초기화
initializeTools(server);

// 원하는 transport와 함께 사용 (stdio, Streamable HTTP 등)
await server.connect(new StdioServerTransport());
```

---

## 개발

로컬에서 개발하거나 테스트할 때 사용하는 명령어입니다.

```bash
# 의존성 설치
bun install

# 개발 모드 실행 (stdio)
bun run dev

# 빌드
bun run build

# 린트
bun run lint

# 타입 검사
bun run typecheck
```

### MCP Inspector로 테스트

[MCP Inspector](https://github.com/modelcontextprotocol/inspector)를 사용하면 브라우저에서 도구를 테스트할 수 있습니다:

```bash
npx @modelcontextprotocol/inspector bun ./dist/stdio.js
```
