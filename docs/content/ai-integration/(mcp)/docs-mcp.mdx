---
title: Docs MCP
description: AI Agent가 SEED 문서에 직접 접근할 수 있는 MCP 서버를 제공합니다.
---

## 소개

[`@seed-design/docs-mcp`](https://github.com/daangn/seed-design/tree/dev/packages/docs-mcp)는 SEED 문서를 위한 공식 MCP(Model Context Protocol) 서버입니다.

이 MCP 서버를 사용하면 AI Agent가 다음 정보에 직접 접근할 수 있습니다:

- **컴포넌트**: 해부도, 속성, 사용 가이드라인
- **React · Lynx · Breeze**: 설치, props, 사용 예시
- **Foundations**: 색상, 타이포그래피, 간격 등 디자인 토큰
- **Rootage**: 디자인 토큰과 컴포넌트 스펙 원본 데이터

Figma 연결 없이 독립적으로 실행되며, SEED를 사용하는 프로젝트에서 AI 도구의 문서 이해도를 높일 수 있습니다.

## 설치

<Tabs items={['Claude Code', 'Codex CLI', 'Gemini CLI', 'Cursor', 'Claude Desktop']}>

<Tab value="Claude Code">

```sh
claude mcp add seed-docs -- npx -y @seed-design/docs-mcp
```

</Tab>

<Tab value="Codex CLI">

```sh
codex mcp add seed-docs -- npx -y @seed-design/docs-mcp
```

</Tab>

<Tab value="Gemini CLI">

`~/.gemini/settings.json` 파일에 다음을 추가합니다:

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"]
    }
  }
}
```

</Tab>

<Tab value="Cursor">

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"],
      "type": "stdio"
    }
  }
}
```

[Cursor MCP에 대해 자세히 알아보기](https://docs.cursor.com/context/model-context-protocol)

</Tab>

<Tab value="Claude Desktop">

`~/Library/Application Support/Claude/claude_desktop_config.json` 파일에 다음을 추가합니다:

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"]
    }
  }
}
```

</Tab>

</Tabs>

## 조회할 문서 사이트 바꾸기

기본값은 `https://seed-design.io`입니다. `SEED_DOCS_BASE_URL` 환경 변수로 로컬 문서 서버나 아카이브 사이트를 겨눌 수 있습니다. MCP 클라이언트 설정에서 `env`로 넘깁니다.

```json
{
  "mcpServers": {
    "seed-docs": {
      "command": "npx",
      "args": ["-y", "@seed-design/docs-mcp"],
      "env": { "SEED_DOCS_BASE_URL": "http://localhost:3000" }
    }
  }
}
```

## 사용 가능한 도구

도구는 호출할 때마다 문서 사이트에서 최신 내용을 읽어오므로, 문서가 추가되거나 옮겨져도 서버를 업데이트할 필요가 없습니다.

### 검색

#### `search_docs`

문서 본문을 검색해 관련 문서를 관련도 순으로 반환합니다. 어느 문서에 답이 있는지 모를 때 사용하며, 결과는 문서 사이트의 검색과 같습니다. 각 결과에는 `get_doc`으로 읽을 수 있는 주소와 제목, 설명이 함께 나옵니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `query` | string | 검색어. 한국어와 영어를 모두 지원합니다 |

**예시:**
```
search_docs({ query: "액션 버튼" })
```

---

### 문서

#### `list_docs`

섹션 안의 문서 목록을 제목, 설명과 함께 반환합니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `section` | string | 섹션 이름. 문서 사이트 주소의 첫 경로입니다 (예: `react`, `components`, `foundations`) |

**예시:**
```
list_docs({ section: "react" })
```

#### `get_doc`

특정 문서의 본문을 반환합니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `section` | string | 섹션 이름 |
| `path` | string | 섹션 안의 문서 경로 (예: `components/action-button`, `color`) |

**예시:**

컴포넌트의 디자인 가이드라인:
```
get_doc({ section: "components", path: "action-button" })
```

React 컴포넌트의 설치 방법, props, 사용 예시:
```
get_doc({ section: "react", path: "components/action-button" })
```

파운데이션 문서:
```
get_doc({ section: "foundations", path: "color" })
```

---

### Rootage

SEED의 Rootage 스펙(디자인 토큰 및 컴포넌트 스펙의 원본 JSON 데이터)에 접근합니다.

#### `get_rootage`

Rootage 리소스를 반환합니다. 경로 없이 호출하면 사용 가능한 모든 리소스 목록을 반환합니다.

**파라미터:**
| 이름 | 타입 | 설명 |
|------|------|------|
| `path` | string (선택) | 리소스 경로 (예: `/color.json`, `/components/action-button.json`) |

**예시:**

리소스 목록 조회:
```
get_rootage()
```

특정 리소스 조회:
```
get_rootage({ path: "/components/action-button.json" })
```

---

## 프로그래매틱 사용

`@seed-design/docs-mcp`는 npm 패키지로 설치하여 커스텀 MCP 서버에 통합할 수 있습니다.

### 설치

`server`는 MCP TypeScript SDK v2의 `McpServer` 인스턴스이므로, transport를 직접 연결하려면 `@modelcontextprotocol/server`도 함께 설치합니다.

```bash
npm install @seed-design/docs-mcp @modelcontextprotocol/server
# 또는
bun add @seed-design/docs-mcp @modelcontextprotocol/server
```

### 사용 예시

```javascript
import { StdioServerTransport } from "@modelcontextprotocol/server/stdio";
import { server } from "@seed-design/docs-mcp/server";
import { initializeTools } from "@seed-design/docs-mcp/tools";

// 도구 초기화
initializeTools(server);

// 원하는 transport와 함께 사용 (stdio, Streamable HTTP 등)
await server.connect(new StdioServerTransport());
```
