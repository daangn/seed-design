---
title: List
description: List 컴포넌트는 정보를 구조화된 목록 형태로 표시하는 데 사용됩니다.
---

import { Component } from "lucide-react";

<ComponentExample name="list-preview">
  ```json doc-gen:file
  {
    "file": "./components/example/list-preview.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

## Installation

```package-install
npx @seed-design/cli@latest add list
```

<ManualInstallation name="list" />

## Props

### `List`

[`VStackProps`](/react/components/layout/v-stack#props)와 동일합니다.

### `ListItem`

<react-type-table path="./registry/ui/list.tsx" name="ListItemProps" />

### `ListItemButton`

<react-type-table path="./registry/ui/list.tsx" name="ListItemButtonProps" />

### `ListItemCheckbox`

<react-type-table path="./registry/ui/list.tsx" name="ListItemCheckboxProps" />

### `ListItemRadio`

<react-type-table path="./registry/ui/list.tsx" name="ListItemRadioProps" />

## Examples

### 기본 사용법

가장 기본적인 형태의 리스트입니다. 제목만 포함된 간단한 리스트 아이템들을 표시합니다.

<ComponentExample name="list-basic">
  ```json doc-gen:file
  {
    "file": "./components/example/list-basic.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Prefix 또는 Suffix가 있는 리스트 항목

예를 들어,

- [Avatar](/react/components/avatar)와 같은 요소를 Prefix 영역에 표시할 수 있습니다.
- [Action Button](/react/components/action-button)이나 아이콘과 같은 요소를 Suffix 영역에 추가할 수 있습니다.

<ComponentExample name="list-affixes">
  ```json doc-gen:file
  {
    "file": "./components/example/list-affixes.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### 리스트 항목 전체를 클릭 가능한 버튼으로 만들기

`ListItemButton`을 사용해서 리스트 항목 전체를 클릭 가능한 버튼으로 만들 수 있습니다.

<ComponentExample name="list-clickable">
  ```json doc-gen:file
  {
    "file": "./components/example/list-clickable.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### 인터랙티브 리스트

체크박스나 라디오 버튼과 같은 폼 요소를 포함한 리스트입니다. `asChild` prop을 사용하여 다른 컴포넌트와 조합할 수 있습니다.

<ComponentExample name="list-interactive">
  ```json doc-gen:file
  {
    "file": "./components/example/list-interactive.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Highlighted

<ComponentExample name="list-highlighted">
  ```json doc-gen:file
  {
    "file": "./components/example/list-highlighted.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Disabled

<ComponentExample name="list-disabled">
  ```json doc-gen:file
  {
    "file": "./components/example/list-disabled.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

## 구조

List 컴포넌트는 다음과 같은 구조로 구성됩니다:

```
List.Root
└── List.Item
    ├── List.Prefix (선택사항)
    ├── List.Content
    │   ├── List.Title
    │   └── List.Detail (선택사항)
    └── List.Suffix (선택사항)
```

### 영역별 역할

- **List.Root**: 전체 리스트를 감싸는 컨테이너 역할
- **List.Item**: 개별 리스트 아이템을 나타내며, 클릭 가능한 영역을 정의
- **List.Prefix**: 아이콘, 아바타, 체크박스 등을 표시하는 왼쪽 영역
- **List.Content**: 주요 콘텐츠가 들어가는 중앙 영역
- **List.Title**: 리스트 아이템의 주제목
- **List.Detail**: 부가 설명이나 세부 정보
- **List.Suffix**: 액션 버튼, 화살표, 상태 표시 등을 위한 오른쪽 영역

## 사용 가이드

### `asChild` prop을 사용하여 적절한 시맨틱 요소와 조합하세요

<Card title="Composition" href="/react/components/concepts/composition#aschild-prop" icon={<Component />}>

`asChild` prop에 대해 자세히 알아봅니다.

</Card>

#### `List.Content`에 `asChild` prop을 사용하는 경우

리스트 항목 전체 영역을 클리커블한 버튼으로 만드는 경우 사용합니다. `List.Item`을 `button`으로 만들지 않도록 유의하세요. `List.Suffix`에 버튼을 넣는 경우 `button`이 중첩되는 문제가 발생합니다.

```tsx
<List.Item>
  {/* [!code highlight] */}
  <List.Content asChild>
    {/* [!code highlight] */}
    <button type="button" onClick={() => alert("사용자 클릭됨")}>
      <List.Title>사용자</List.Title>
      {/* [!code highlight] */}
    </button>
    {/* [!code highlight] */}
  </List.Content>
  <List.Suffix>
    <ActionButton
      size="xsmall"
      variant="brandSolid"
      onClick={() => alert("보기 클릭됨")}
    >
      보기
    </ActionButton>
  </List.Suffix>
</List.Item>
```

[Snippet](/react/components/concepts/snippet)으로 제공되는 `ListItemButton`은 이 패턴을 쉽게 구현할 수 있도록 돕습니다.

```tsx
<ListItemButton
  onClick={() => alert("사용자 클릭됨")}
  title="사용자"
  detail="항목 6개"
  suffix={
    <ActionButton
      size="xsmall"
      variant="brandSolid"
      onClick={() => alert("보기 클릭됨")}
    >
      보기
    </ActionButton>
  }
/>
```

#### `List.Item`에 `asChild` prop을 사용하는 경우

리스트 항목 전체 영역을 `label`로 만들고, `List.Suffix`에 라디오 버튼, 체크박스 등 `input`을 넣는 경우 사용합니다.

```tsx
{/* [!code highlight] */}
<List.Item asChild> {/* <label> */}
  {/* [!code highlight] */}
  <Checkbox.Root.Primitive defaultChecked>
    <List.Content>
      <List.Title>동의</List.Title>
    </List.Content>
    <List.Suffix>
      <Checkmark />
      <Checkbox.HiddenInput />
    </List.Suffix>
    {/* [!code highlight] */}
  </Checkbox.Root.Primitive>
  {/* [!code highlight] */}
</List.Item>
```

[Snippet](/react/components/concepts/snippet)으로 제공되는 `ListItemCheckbox` 및 `ListItemRadio`는 이 패턴을 쉽게 구현할 수 있도록 돕습니다.

```tsx
<ListItemCheckbox defaultChecked title="동의" />
```

### 클릭 가능한 아이템의 경우 키보드 접근성을 고려하세요

### 스크린 리더를 위해 적절한 aria 속성을 추가하세요

### 성능 최적화

- 긴 리스트의 경우 가상화를 고려하세요
- 배열을 바탕으로 리스트 항목을 생성하는 경우 불필요한 리렌더링을 방지하기 위해 `key` prop을 적절히 사용하세요
