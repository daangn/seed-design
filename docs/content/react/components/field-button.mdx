---
title: Field Button
description: "이 문서는 정리 중이에요. 문의 내용은 #_design_core 채널을 찾아주세요."
---

<ComponentExample name="field-button-preview">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-preview.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

## Installation

```package-install
npx @seed-design/cli@latest add field-button
```

<ManualInstallation name="field-button" />

## Props

### `FieldButton`

<react-type-table
  path="./registry/ui/field-button.tsx"
  name="FieldButtonProps"
/>

### `FieldButtonValue`

<react-type-table
  path="./registry/ui/field-button.tsx"
  name="FieldButtonValueProps"
/>

### `FieldButtonPlaceholder`

<react-type-table
  path="./registry/ui/field-button.tsx"
  name="FieldButtonPlaceholderProps"
/>

## Examples

### 폼 요소로 활용하기

`FieldButton`에 `values` prop을 제공하는 경우 폼 제출에 활용할 수 있도록 `<input type="hidden" />`을 함께 렌더링합니다.

<ComponentExample name="field-button-form-control">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-form-control.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Clear Button 표시하기

`showClearButton` prop을 `true`로 설정하면 Clear Button이 표시됩니다. `showClearButton`을 `true`로 설정하는 경우 `onValuesChange` prop을 제공해야 합니다.

<ComponentExample name="field-button-clear-button">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-clear-button.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### `FieldButtonValue`와 `FieldButtonPlaceholder` 활용하기

`<FieldButton>` 내부에 `children`으로 넣어 사용할 수 있는 단순한 `<div>` 요소입니다.

스크린 리더는 `FieldButtonValue`와 `FieldButtonPlaceholder`를 읽지 않습니다. `buttonProps["aria-label"]`에 적절한 값을 제공하여 접근성을 확보하세요. 자세한 내용은 [접근 가능하게 만들기](#접근-가능하게-만들기) 섹션을 참고하세요.

<ComponentExample name="field-button-value-placeholder">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-value-placeholder.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### 접근 가능하게 만들기

**Field Button 내부 `button`에 `aria-label` 속성을 제공하여**, 스크린 리더 사용자가 Field Button을 클릭했을 때 어떤 일이 일어날지 예상할 수 있도록 해야 하며, 현재 선택된 값이 있는 경우 그 값이 무엇인지도 알려야 합니다.

```tsx
<FieldButton
  label="사용자 이름" // 스크린 리더는 label을 읽습니다.
  description="본명을 사용하지 않아도 괜찮습니다." // 스크린 리더는 description을 버튼과 함께 읽습니다.
  buttonProps={{
    // [!code highlight]
    // 버튼을 클릭했을 때 어떤 일이 일어날지, 현재 선택된 값이 무엇인지 알려야 합니다.
    "aria-label": `사용자 이름 선택 화면 열기. 현재 선택된 이름: ${username || "없음"}`, // [!code highlight]
    "aria-haspopup": "dialog",
    onClick: () => setIsUsernamePickerOpen(true),
  }}
>
  {username ? (
    <FieldButtonValue>{username}</FieldButtonValue> // 스크린 리더는 FieldButtonValue를 읽지 않습니다.
  ) : (
    <FieldButtonPlaceholder>김하늘</FieldButtonPlaceholder> // 스크린 리더는 FieldButtonPlaceholder를 읽지 않습니다.
  )}
</FieldButton>
```

화면을 볼 수 있는 사용자는 Field Button이 클릭할 수 있는 요소라는 점과, Field Button을 클릭했을 때 일어날 동작을 쉽게 예측할 수 있지만, 스크린 리더 사용자는 `button`에 `aria-label` 속성이 없는 경우 Field Button을 클릭했을 때 어떤 일이 일어날지 파악하기 어렵습니다.

### State

#### Enabled

<ComponentExample name="field-button-enabled">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-enabled.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Disabled

<ComponentExample name="field-button-disabled">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-disabled.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Customizable Parts

#### Prefix

<ComponentExample name="field-button-prefix">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-prefix.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Suffix

<ComponentExample name="field-button-suffix">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-suffix.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Both Affixes

<ComponentExample name="field-button-both-affixes">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-both-affixes.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Indicator

<ComponentExample name="field-button-indicator">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-indicator.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

### Use Cases

#### Form and Bottom Sheet

<ComponentExample name="field-button-form-bottom-sheet">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-form-bottom-sheet.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### React Hook Form

<ComponentExample name="field-button-react-hook-form">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-react-hook-form.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Multiple Values (React Hook Form (Field Array))

<ComponentExample name="field-button-field-array">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-field-array.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>

#### Multiple Values (Native `form`)

<ComponentExample name="field-button-multiple-values">
  ```json doc-gen:file
  {
    "file": "./components/example/field-button-multiple-values.tsx",
    "codeblock": true
  }
  ```
</ComponentExample>
