사용 가능 버전: @seed-design/react@2.0.0, @seed-design/css@2.0.0

## Preview

```tsx
import { ActionButton } from "seed-design/ui/action-button";

export default function ActionButtonPreview() {
  return <ActionButton>라벨</ActionButton>;
}
```

## Preview

```tsx
import { Badge } from "@seed-design/lynx-react";

root.render(<Badge>거래 가능</Badge>);
```

## Installation

- npm: npx @seed-design/cli@latest add ui:action-button
- pnpm: pnpm dlx @seed-design/cli@latest add ui:action-button

* `variant`
  - type: `string`

Component spec (JSON): /rootage/components/action-button.json

- [Loading](https://seed-design.io/llms/docs/patterns/loading.txt) — 로딩을 관리하는 데 필요한 요소, 시간에 따른 적합한 선택지와 단계별로 사용자 경험을 최적화하는 방법에 대해 소개합니다.
