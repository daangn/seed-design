---
"@seed-design/css": patch
---

`AppBar`에 적용되는 그라디언트를 `AppScreen`으로 이동합니다.

- `<AppBar tone="transparent">`를 사용하는 경우 `tone="transparent"`를 `AppScreen`으로 옮겨 그라디언트를 표시할 수 있습니다.
- `AppScreen`에 `gradient={false}`를 설정하여 그라디언트를 숨길 수 있습니다.
