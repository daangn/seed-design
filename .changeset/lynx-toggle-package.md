---
"@seed-design/lynx-toggle": minor
---

Lynx 토글(pressed) 상태와 탭 인터랙션을 다루는 `useToggle` 훅을 추가했습니다. `pressed`(on/off), `active`(누르는 중), `rootProps`(bindtap·터치 핸들러)를 제공하며 controlled/uncontrolled를 모두 지원합니다.
