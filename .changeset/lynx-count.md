---
"@seed-design/lynx-react": minor
---

feat(lynx-react): add Count component

숫자 값을 강조해 표시하는 `Count` 컴포넌트를 추가했습니다. 네이티브 `<text>` 요소에
`seed-count__text` 클래스를 적용합니다. 웹(`@seed-design/react`)의 `Count`와 동일한
역할이지만, Lynx는 `<span>` 래퍼 대신 텍스트 요소 직접 스타일링 구조를 사용합니다.
