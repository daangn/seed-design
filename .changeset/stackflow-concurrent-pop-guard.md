---
"@seed-design/stackflow": patch
---

전환(transition) 애니메이션이 진행 중일 때 들어온 추가 `pop()`을 무시합니다 ("전환당 1회"). 백버튼 연타, 중복 호출 등 어느 UI에서 온 동시 pop이든 호출부와 무관하게 막아, 화면이 의도보다 많이 닫히거나 exit 애니메이션이 겹쳐 깨지던 문제를 방지합니다. 여러 화면을 한 번에 닫으려면 `pop(count)` 를 사용하세요 (`pop(count)` 는 정상 동작합니다).
