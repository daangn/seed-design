---
"@seed-design/cli": patch
---

텔레메트리 안내를 stdout이 아니라 stderr로 내보냅니다.

세션당 한 번 나오는 `📊 사용 데이터 수집 중 ...` 안내가 명령어 결과와 같은 stdout으로 나가고 있었습니다. `seed-design docs <경로> --raw`처럼 출력을 그대로 파이프로 넘기는 경우, 문서 본문 뒤에 이 안내가 붙어서 받는 쪽이 문서의 일부로 읽었습니다. 안내 문구와 표시 시점은 그대로입니다.
