{
  "name": "@seed-design/figma-contrast-checker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "bun run tsc && bun run build",
    "tsc": "tsc --noEmit -p widget-src",
    "build": "bun build.mjs",
    "dev": "concurrently -n tsc,build 'bun tsc --preserveWatchOutput --watch' 'bun dev.mjs'"
  },
  "author": "Asher <asher@daangn.com>",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "@figma/widget-typings": "*",
    "@types/apca-w3": "^0.1.3",
    "concurrently": "^6.3.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "apca-w3": "^0.1.9"
  }
}
