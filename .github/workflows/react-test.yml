name: Test React

on:
  push:
    paths:
      - "packages/react/**/*"
      - ".github/workflows/react-test.yml"
      - ".github/actions/setup/action.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup

      - name: Build common dependencies
        run: bun --filter @ride-developer/dom-utils build && bun headless:build && bun --filter @ride-developer/react build

      - name: Run tests
        run: bun react:test
