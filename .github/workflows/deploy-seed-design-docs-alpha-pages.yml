on:
  push:
    branches:
      - "**"
      - "!main"
      - "!dev" # V3 deployment; remove this when V3 is stable
    paths:
      - "docs/**"
      - "packages/css/**"
      - "packages/react/**"
      - "packages/stackflow/**"
      - ".github/workflows/deploy-seed-design-docs-alpha-pages.yml"
      - ".github/actions/setup/action.yml"
  workflow_dispatch:
    inputs:
      skip-figma-cache:
        description: "Skip Figma image cache (fetch fresh images)"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  pull-requests: write

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  FIGMA_FILE_KEY: ${{ secrets.FIGMA_FILE_KEY }}
  FIGMA_PERSONAL_ACCESS_TOKEN: ${{ secrets.FIGMA_PERSONAL_ACCESS_TOKEN }}

name: deploy-seed-design-docs-alpha-pages

jobs:
  deploy:
    name: deploy-seed-design-docs-alpha-pages
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Restore Figma image cache
        if: ${{ !inputs.skip-figma-cache }}
        uses: actions/cache/restore@v5
        with:
          path: docs/.cache/figma-image
          key: ${{ runner.os }}-figma-images-${{ hashFiles('docs/.cache/figma-image/**') }}
          restore-keys: |
            ${{ runner.os }}-figma-images-

      - name: Restore Next.js cache
        uses: actions/cache@v5
        with:
          path: docs/.next/cache
          # Generate a new cache whenever packages or source files change.
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/bun.lock') }}-${{ hashFiles('**/*.js', '**/*.jsx', '**/*.ts', '**/*.tsx') }}
          # If source files changed but packages didn't, rebuild from a prior cache.
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/bun.lock') }}-

      - uses: ./.github/actions/setup
        with:
          build-packages: true
          build-rootage: true

      - name: Build Docs
        working-directory: ./docs
        env:
          FIGMA_CACHE_DISABLED: ${{ inputs.skip-figma-cache && '1' || '0' }}
        run: |
          bun run build

      - name: Save Figma image cache
        uses: actions/cache/save@v5
        with:
          path: docs/.cache/figma-image
          key: ${{ runner.os }}-figma-images-${{ hashFiles('docs/.cache/figma-image/**') }}

      - name: Build for Cloudflare Workers (OpenNext)
        working-directory: ./docs
        run: bun run cf:build

      - name: Upload Worker Version (Alpha Preview)
        id: deploy
        working-directory: ./docs
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          BRANCH_ALIAS=$(echo "${{ github.ref_name }}" | sed 's/[^a-zA-Z0-9-]/-/g' | head -c 63)
          bun wrangler versions upload --preview-alias "$BRANCH_ALIAS"
          echo "preview_url=https://${BRANCH_ALIAS}-seed-design-io.${{ secrets.CF_WORKERS_SUBDOMAIN }}.workers.dev" >> "$GITHUB_OUTPUT"

      - name: Find related PR
        id: pr
        uses: actions/github-script@v8
        with:
          script: |
            const { owner, repo } = context.repo;
            if (!context.ref.startsWith('refs/heads/')) {
              core.setOutput('number', '');
              return;
            }

            const branch = context.ref.replace('refs/heads/', '');
            const pulls = await github.rest.pulls.list({
              owner,
              repo,
              state: 'open',
              head: `${owner}:${branch}`,
              per_page: 1,
            });

            core.setOutput('number', pulls.data[0] ? String(pulls.data[0].number) : '');

      - name: Upsert PR preview comment
        if: ${{ steps.pr.outputs.number != '' }}
        uses: actions/github-script@v8
        env:
          PR_NUMBER: ${{ steps.pr.outputs.number }}
          PREVIEW_URL: ${{ steps.deploy.outputs.preview_url }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const issue_number = Number(process.env.PR_NUMBER);
            const marker = '<!-- seed-design-preview:docs-alpha -->';
            const previewUrl = (process.env.PREVIEW_URL || '').trim();

            const body = [
              marker,
              '## Alpha Preview (Docs)',
              '',
              `- Preview URL: ${previewUrl || '(not available)'}`,
              `- Commit: \`${context.sha.slice(0, 7)}\``,
            ].join('\n');

            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100,
            });

            const existing = comments.find((comment) =>
              comment.user?.type === 'Bot' &&
              comment.body?.includes(marker)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body,
              });
            }
              