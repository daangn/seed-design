export const getLabelId = (id: string) => `radio-group:${id}:label`;
export const getDescriptionId = (id: string) => `radio-group:${id}:description`;
export const getErrorMessageId = (id: string) => `radio-group:${id}:error-message`;
