type ColorVariant = (
  | 'system'
  | 'light'
  | 'dark'
);

type PlatformVariant = (
  | 'unknown'
  | 'ios'
  | 'android'
);

export type ColorMode = (
  | 'auto'
  | 'light-only'
  | 'dark-only'
);
