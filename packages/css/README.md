# @seed-design/css

CSS implementation of Seed Design, providing design tokens and component styles and theming codes.

## Stability

`@seed-design/css/vars/component`에서는 `typography`만 제공합니다.

다른 컴포넌트의 구현용 값은 공개 API가 아니므로 제공하지 않습니다. 값이 필요하면 디자인 토큰(`@seed-design/css/vars`)을 사용하세요.
