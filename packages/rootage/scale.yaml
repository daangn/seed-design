kind: Tokens
metadata:
  id: scale
  name: Scale
data:
  collection: motion
  tokens:
    # TODO: remove all three — superseded by the dynamic scale that scale feedback derives at runtime,
    # and no component spec references them.
    $scale.s95:
      values:
        preferred: 0.95
        reduced: 1
    $scale.s97:
      values:
        preferred: 0.97
        reduced: 1
    $scale.s98:
      values:
        preferred: 0.98
        reduced: 1
