# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: avatar
  name: Avatar
data:
  schema:
    slots:
      root:
        properties:
          cornerRadius:
            type: dimension
          size:
            type: dimension
      badge:
        properties:
          cornerRadius:
            type: dimension
          color:
            type: color
          size:
            type: dimension
  definitions:
    base:
      enabled:
        root:
          cornerRadius: $radius.full
        badge:
          cornerRadius: $radius.full
          color: $color.bg.layer-default
    size=20:
      enabled:
        root:
          size: 20px
        badge:
          size: 10px
    size=24:
      enabled:
        root:
          size: 24px
        badge:
          size: 12px
    size=36:
      enabled:
        root:
          size: 36px
        badge:
          size: 16px
    size=48:
      enabled:
        root:
          size: 48px
        badge:
          size: 24px
    size=64:
      enabled:
        root:
          size: 64px
        badge:
          size: 24px
    size=80:
      enabled:
        root:
          size: 80px
        badge:
          size: 24px
    size=96:
      enabled:
        root:
          size: 96px
        badge:
          size: 32px
