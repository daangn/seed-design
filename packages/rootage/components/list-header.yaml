# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: list-header
  name: List Header
data:
  schema:
    slots:
      root:
        properties:
          paddingX:
            type: dimension
          paddingY:
            type: dimension
          gap:
            type: dimension
          fontSize:
            type: dimension
          lineHeight:
            type: dimension
          fontWeight:
            type: number
          color:
            type: color
  definitions:
    base:
      enabled:
        root:
          paddingX: $dimension.spacing-x.global-gutter
          paddingY: $dimension.x2
          gap: $dimension.x2 # TODO: Figma에 추가 필요
          fontSize: $font-size.t4
          lineHeight: $line-height.t4
          fontWeight: $font-weight.medium
          color: $color.fg.neutral-subtle # TODO: variant로 빠져야함
