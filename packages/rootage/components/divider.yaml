# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: divider
  name: Divider
data:
  schema:
    slots:
      root:
        properties:
          thickness:
            type: dimension
  rules:
    - slots:
        root:
          thickness: 1px
