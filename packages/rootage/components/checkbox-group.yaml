# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: checkbox-group
  name: Checkbox Group
data:
  schema:
    slots:
      root:
        properties:
          gapY:
            type: dimension
  rules:
    - slots:
        root:
          gapY: $dimension.x1
