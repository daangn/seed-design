# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: image-frame-overlay-positioner
  name: Image Frame Overlay Positioner
data:
  schema:
    slots:
      root:
        properties:
          inset:
            type: dimension
            description: 오버레이가 이미지 프레임 가장자리에서 떨어진 거리입니다.
  definitions:
    base:
      enabled:
        root:
          inset: 6px
