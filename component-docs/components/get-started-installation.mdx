import { Steps, Tabs } from 'nextra/components'

<Tabs items={['yarn', 'pnpm', 'npm']}>
  <Tabs.Tab>
    ```sh copy
    yarn add @seed-design/stylesheet @seed-design/recipe
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```sh copy
    pnpm add @seed-design/stylesheet @seed-design/recipe
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```sh copy
    npm install @seed-design/stylesheet @seed-design/recipe
    ```
  </Tabs.Tab>
</Tabs>
